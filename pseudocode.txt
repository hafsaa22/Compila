OPTION: java;

VAR age : entier;
VAR estMajeur : boolean;
VAR lettre : chaine;
VAR compteur : chaine;

AFFICHER("Veuillez saisir votre age :");
LIRE(age);

AFFICHER(10 / 2);
AFFICHER(2 - 1);
AFFICHER(6 * 3);
AFFICHER(1024 % 5);

AFFICHER(true ET false);
AFFICHER(true OU false);

SI age >= 18 ALORS
    estMajeur = true;
    lettre = 'M';
SINON
    estMajeur = false;
    lettre = 'J';
FINSI;

SI estMajeur == true ALORS
    AFFICHER("Acces autorise. Statut :");
    AFFICHER(lettre);
SINON
    AFFICHER("Acces refuse.");
FINSI;

compteur = 0;
TANT_QUE compteur < 3 FAIRE
    AFFICHER(compteur);
    compteur = compteur + 1;
FINTANTQUE;
